name: Déploiement backend Supabase (SQL + Edge Functions)

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Exécuter le SQL (colonnes profile_completed / reminder_sent_at)
        run: |
          sudo apt-get update -y && sudo apt-get install -y postgresql-client
          psql "${{ secrets.SUPABASE_DB_URL }}" -v ON_ERROR_STOP=1 -f supabase/migrations_manuelles/quick-lead-profile-completed.sql

      - name: Installer la CLI Supabase
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Déployer link-quick-lead-account
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy link-quick-lead-account --project-ref bvvbkaluajgdurxnnqqu

      - name: Déployer send-profile-reminder
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy send-profile-reminder --project-ref bvvbkaluajgdurxnnqqu

      - name: Résumé
        run: echo "✅ SQL exécuté + 2 edge functions déployées avec succès."
